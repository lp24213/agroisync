version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "🔧 Configurando ambiente Frontend..."
        - node --version
        - npm --version
        - echo "📦 Instalando dependências Frontend..."
        - npm install --legacy-peer-deps
        - echo "✅ Dependências Frontend instaladas"
    build:
      commands:
        - echo "🏗️ Building aplicação Next.js..."
        - npm run build
        - echo "✅ Build Frontend concluído"
        - echo "📋 Verificando diretório de saída..."
        - ls -la out/
        - echo "📁 Conteúdo do diretório out:"
        - find out/ -type f | head -20
    postBuild:
      commands:
        - echo "🎉 Frontend pronto para deploy!"
  artifacts:
    baseDirectory: out
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*
      - out/**/*

backend:
  phases:
    preBuild:
      commands:
        - echo "🔧 Configurando ambiente Backend..."
        - cd backend
        - echo "📦 Instalando dependências Backend..."
        - npm install
        - echo "✅ Dependências Backend instaladas"
    build:
      commands:
        - echo "🏗️ Building Backend Node.js..."
        - npm run build
        - echo "✅ Build Backend concluído"
        - echo "📋 Verificando diretório de saída..."
        - ls -la dist/
        - echo "📁 Conteúdo do diretório dist:"
        - find dist/ -type f | head -20
    postBuild:
      commands:
        - echo "🎉 Backend buildado com sucesso!"
        - cd ..
  artifacts:
    baseDirectory: backend/dist
    files:
      - '**/*'
  cache:
    paths:
      - backend/node_modules/**/*
      - backend/dist/**/*
