version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "🚀 Iniciando deploy profissional do AgroSync..."
        - echo "📁 Navegando para o diretório frontend..."
        - cd frontend
        - echo "📦 Instalando dependências do frontend..."
        - npm ci --prefer-offline --no-audit
        - echo "🔍 Verificando versões..."
        - node --version
        - npm --version
        - echo "🔧 Configurando variáveis de ambiente..."
        - echo "REACT_APP_API_URL=$REACT_APP_API_URL" >> .env.production
        - echo "REACT_APP_STRIPE_PUBLISHABLE_KEY=$REACT_APP_STRIPE_PUBLISHABLE_KEY" >> .env.production
        - echo "REACT_APP_WEB3_PROVIDER=$REACT_APP_WEB3_PROVIDER" >> .env.production
        - echo "REACT_APP_CHATBOT_API_KEY=$REACT_APP_CHATBOT_API_KEY" >> .env.production
    build:
      commands:
        - echo "🏗️ Construindo aplicação frontend profissional..."
        - CI=false npm run build
        - echo "✅ Build do frontend concluído com sucesso!"
        - echo "📊 Verificando tamanho do build..."
        - du -sh build/
        - echo "🔍 Verificando arquivos gerados..."
        - ls -la build/
  artifacts:
    baseDirectory: frontend/build
    files:
      - '**/*'
  cache:
    paths:
      - frontend/node_modules/**/*
      - frontend/.npm/**/*
      - frontend/build/**/*