version: 1
applications:
  - appRoot: frontend
    env:
      variables:
        NODE_ENV: production
        NPM_CONFIG_PRODUCTION: false
        NPM_CONFIG_AUDIT: false
        NODE_OPTIONS: "--max-old-space-size=4096"
    frontend:
      phases:
        preBuild:
          commands:
            - echo "🚀 INICIANDO BUILD AGROISYNC.COM"
            - echo "📦 Instalando dependências..."
            - cd ..
            - npm ci --no-audit --prefer-offline --legacy-peer-deps
            - cd frontend
            - echo "🧹 Limpando cache..."
            - rm -rf .next out dist node_modules package-lock.json
            - echo "📥 Instalando dependências frontend..."
            - npm ci --production=false --include=dev --no-audit --prefer-offline --legacy-peer-deps
            - echo "✅ Dependências instaladas com sucesso!"
        build:
          commands:
            - echo "🔨 Iniciando build Next.js..."
            - npm run build
            - echo "✅ Build concluído com sucesso!"
        postBuild:
          commands:
            - echo "📁 Verificando arquivos gerados..."
            - ls -la .next
            - echo "📊 Estatísticas do build:"
            - du -sh .next
            - echo "🎉 BUILD AGROISYNC.COM CONCLUÍDO!"
      artifacts:
        baseDirectory: .next
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**
          - .next/cache/**
          - ../node_modules/**
          - ~/.npm/**
          - ~/.cache/**
