version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "🔧 Configurando ambiente Frontend + Backend..."
        - node --version
        - npm --version
        - echo "📦 Instalando dependências..."
        - npm install --legacy-peer-deps
        - echo "✅ Dependências instaladas"
    build:
      commands:
        - echo "🏗️ Building aplicação Next.js (Frontend + Backend)..."
        - npm run build
        - echo "✅ Build concluído"
        - echo "📋 Verificando diretório de saída..."
        - ls -la .next/
        - echo "📁 Conteúdo do diretório .next:"
        - find .next/ -type f | head -20
        - echo "🔍 Verificando arquivos principais..."
        - ls -lh .next/server/pages-manifest.json
        - echo "🔍 Verificando APIs..."
        - ls -lh .next/server/pages/api/
    postBuild:
      commands:
        - echo "🎉 Deploy pronto!"
        - echo "🚀 Frontend + Backend integrados funcionando!"
        - echo "📁 Estrutura final do diretório .next:"
        - tree .next/ || find .next/ -type f | sort
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*
      - .next/**/*
