version: 1
applications:
  - appRoot: frontend
    env:
      variables:
        NODE_ENV: production
        NPM_CONFIG_PRODUCTION: false
        NPM_CONFIG_AUDIT: false
        NODE_OPTIONS: "--max-old-space-size=4096"
        NODE_VERSION: "20.15.1"
        NPM_VERSION: "10.8.2"
    frontend:
      phases:
        preBuild:
          commands:
            - echo "🚀 INICIANDO BUILD PROFISSIONAL AGROISYNC.COM"
            - echo "📦 Verificando versão do Node.js..."
            - node --version
            - npm --version
            - echo "📦 Instalando dependências root..."
            - cd ..
            - npm ci --no-audit --prefer-offline --legacy-peer-deps
            - echo "📥 Instalando dependências frontend..."
            - cd frontend
            - rm -rf .next out dist node_modules package-lock.json
            - npm ci --production=false --include=dev --no-audit --prefer-offline --legacy-peer-deps
            - echo "✅ Dependências instaladas com sucesso!"
        build:
          commands:
            - echo "🔨 Iniciando build Next.js profissional..."
            - npm run build
            - echo "✅ Build concluído com sucesso!"
        postBuild:
          commands:
            - echo "📁 Verificando arquivos gerados..."
            - ls -la out
            - echo "📊 Estatísticas do build:"
            - du -sh out
            - echo "🎉 BUILD PROFISSIONAL AGROISYNC.COM CONCLUÍDO!"
      artifacts:
        baseDirectory: out
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**
          - out/**
          - ../node_modules/**
          - ~/.npm/**
          - ~/.cache/**
