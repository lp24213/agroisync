version: 1
applications:
  - appRoot: frontend
    frontend:
      phases:
        preBuild:
          commands:
            - echo "🚀 AGROISYNC.COM - BUILD ULTRA PERFEITO INICIADO"
            - echo "📅 Data: $(date)"
            - echo "🔄 Versão: 2.3.1"
            - echo "🌐 Domínio: agroisync.com"
            - echo "🎯 Objetivo: ZERO ERROS - 100% FUNCIONAL - ULTRA PERFEITO"
            - echo "=== INSTALANDO NODE 20.15.1 ==="
            - nvm install 20.15.1
            - nvm use 20.15.1
            - node --version
            - npm --version
            - echo "=== LIMPANDO CACHE ANTERIOR ==="
            - rm -rf .next out dist node_modules package-lock.json
            - echo "=== INSTALANDO TODAS AS DEPENDÊNCIAS ==="
            - npm ci --production=false --include=dev --no-audit --prefer-offline --legacy-peer-deps
            - echo "=== VERIFICANDO INSTALAÇÕES CRÍTICAS ==="
            - npx tsc --version
            - npx eslint --version
            - npx next --version
            - echo "=== VERIFICANDO ESTRUTURA ==="
            - ls -la
            - echo "=== VERIFICANDO PACKAGE.JSON ==="
            - cat package.json | grep -E "(name|version|scripts)"
            - echo "=== DEPENDÊNCIAS INSTALADAS COM SUCESSO ==="
        build:
          commands:
            - echo "=== INICIANDO BUILD PRODUÇÃO ==="
            - echo "🔨 Compilando Next.js para agroisync.com..."
            - npm run build
            - echo "=== VERIFICANDO BUILD ==="
            - ls -la .next
            - echo "=== VERIFICANDO ARQUIVOS ESSENCIAIS ==="
            - test -d .next && echo "✅ .next directory encontrado" || echo "❌ .next directory não encontrado"
            - test -f .next/server.js && echo "✅ server.js encontrado" || echo "❌ server.js não encontrado"
            - test -d .next/static && echo "✅ static files encontrados" || echo "❌ static files não encontrados"
            - echo "=== BUILD CONCLUÍDO COM SUCESSO ==="
        postBuild:
          commands:
            - echo "=== VERIFICAÇÃO PÓS-BUILD ==="
            - echo "📁 Verificando arquivos essenciais..."
            - test -d .next && echo "✅ .next directory encontrado" || echo "❌ .next directory não encontrado"
            - test -f .next/server.js && echo "✅ server.js encontrado" || echo "❌ server.js não encontrado"
            - test -d .next/static && echo "✅ static files encontrados" || echo "❌ static files não encontrados"
            - echo "=== VERIFICANDO TAMANHO DO BUILD ==="
            - du -sh .next
            - echo "=== BUILD VERIFICADO E APROVADO ==="
            - echo "🎉 AGROISYNC.COM - BUILD ULTRA PERFEITO CONCLUÍDO!"
      artifacts:
        # SSR: o Amplify detecta automaticamente a saída do Next; manter .next visível
        baseDirectory: .next
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**
          - .next/cache/**
    customHeaders:
      - pattern: '**/*'
        headers:
          - key: Strict-Transport-Security
            value: max-age=31536000; includeSubDomains; preload
          - key: X-Content-Type-Options
            value: nosniff
          - key: Referrer-Policy
            value: strict-origin-when-cross-origin
          - key: Permissions-Policy
            value: geolocation=(), microphone=(), camera=()
