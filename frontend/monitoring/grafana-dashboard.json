{
  "dashboard": {
    "id": null,
    "title": "AGROTM API Metrics",
    "panels": [
      {
        "type": "graph",
        "title": "HTTP Request Duration (ms)",
        "targets": [
          {
            "expr": "http_request_duration_ms_bucket",
            "legendFormat": "{{le}} ms"
          }
        ],
        "datasource": "Prometheus",
        "xaxis": { "mode": "time" },
        "yaxes": [ { "format": "ms" }, { "format": "short" } ]
      }
    ],
    "schemaVersion": 16,
    "version": 1
  }
}
