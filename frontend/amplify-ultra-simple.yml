version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "🚀 Iniciando build do AGROISYNC Frontend..."
        - node --version
        - npm --version
        - echo "📦 Instalando dependências..."
        - npm ci
        - echo "⚙️ Configurando para Amplify..."
        - cp next.config-final.js next.config.js
        - cp tsconfig-amplify.json tsconfig.json
        - echo "🔧 Configurações aplicadas"
    build:
      commands:
        - echo "🏗️ Buildando aplicação..."
        - npm run build
        - echo "📁 Verificando estrutura..."
        - pwd
        - ls -la
        - echo "📁 Verificando pasta out..."
        - ls -la out/
        - echo "📄 Verificando index.html..."
        - cat out/index.html | head -5
        - echo "✅ Build concluído!"
  artifacts:
    baseDirectory: out
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*
