@import url('https://fonts.googleapis.com/css2?family=Orbitron:wght@400;500;600;700;800;900&display=swap');
@import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap');
@import url('https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@300;400;500;600;700&display=swap');
@import './premium.css';

@tailwind base;
@tailwind components;
@tailwind utilities;

/* Custom CSS Variables */
:root {
  /* AGROSYNC GLOBAL VARIABLES */
  --agro-bg-primary: #0a0a0a;
  --agro-bg-secondary: #1a1a1a;
  --agro-bg-card: #262626;
  --agro-bg-glass: rgba(26, 26, 26, 0.8);
  
  --agro-text-primary: #ffffff;
  --agro-text-secondary: #e5e7eb;
  --agro-text-tertiary: #9ca3af;
  --agro-text-muted: #6b7280;
  
  --agro-accent-emerald: #10b981;
  --agro-accent-emerald-light: #34d399;
  --agro-accent-emerald-dark: #059669;
  --agro-accent-sky: #0ea5e9;
  --agro-accent-sky-light: #38bdf8;
  --agro-accent-amber: #f59e0b;
  --agro-accent-amber-light: #fbbf24;
  
  --agro-border-primary: rgba(255, 255, 255, 0.1);
  --agro-border-secondary: rgba(255, 255, 255, 0.05);
  --agro-border-accent: rgba(16, 185, 129, 0.2);
  
  --shadow-card: 0 1px 3px 0 rgba(0, 0, 0, 0.1), 0 1px 2px 0 rgba(0, 0, 0, 0.06);
  --shadow-elevated: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
  --shadow-card-dark: 0 1px 3px 0 rgba(0, 0, 0, 0.3), 0 1px 2px 0 rgba(0, 0, 0, 0.2);
  --shadow-elevated-dark: 0 10px 15px -3px rgba(0, 0, 0, 0.3), 0 4px 6px -2px rgba(0, 0, 0, 0.2);
}

/* Dark mode overrides */
.dark {
  --shadow-card: var(--shadow-card-dark);
  --shadow-elevated: var(--shadow-elevated-dark);
}

/* Custom Components */
@layer components {
  .shadow-card {
    box-shadow: var(--shadow-card);
  }
  
  .shadow-elevated {
    box-shadow: var(--shadow-elevated);
  }

  /* NOVAS CLASSES PREMIUM AGROSYNC */
  
  /* Header Premium */
  .header-premium {
    @apply relative overflow-hidden;
  }
  
  /* Section Premium */
  .section-premium {
    @apply py-20 px-4 relative overflow-hidden;
  }
  
  .section-premium-compact {
    @apply py-16 px-4 relative overflow-hidden;
  }
  
  /* Card Premium */
  .card-premium {
    @apply bg-white/95 backdrop-blur-sm rounded-2xl shadow-lg border border-slate-200/50 hover:shadow-xl transition-all duration-300 relative overflow-hidden;
  }
  
  /* Hover Lift Effect */
  .hover-lift {
    @apply transition-all duration-300 hover:shadow-2xl hover:-translate-y-2;
  }
  
  /* Title Premium */
  .title-premium {
    @apply bg-gradient-to-r from-slate-800 via-emerald-700 to-yellow-600 bg-clip-text text-transparent;
  }
  
  .title-premium-small {
    @apply bg-gradient-to-r from-slate-700 via-emerald-600 to-yellow-500 bg-clip-text text-transparent;
  }
  
  /* Text Gradient Premium */
  .text-gradient-premium {
    @apply bg-gradient-to-r from-slate-700 via-emerald-600 to-yellow-500 bg-clip-text text-transparent;
  }
  
  /* Button Accent Green */
  .btn-accent-green {
    @apply bg-gradient-to-r from-emerald-500 via-emerald-600 to-emerald-700 text-white font-bold rounded-xl hover:from-emerald-600 hover:via-emerald-700 hover:to-emerald-800 transition-all duration-300 shadow-lg hover:shadow-xl relative overflow-hidden;
  }

  /* TEMA AGRONEGÓCIO PREMIUM - CLASSES UTILITÁRIAS */
  
  /* Cards com tema agro */
  .card {
    @apply bg-agro-bg-card rounded-2xl shadow-xl border border-agro-border-primary backdrop-blur-sm;
  }
  
  /* Botões primários */
  .btn-primary {
    @apply bg-gradient-to-r from-agro-accent-emerald to-agro-accent-emerald-dark text-white font-semibold rounded-2xl px-6 py-3 hover:shadow-lg transition-all duration-300 focus:outline-none focus:ring-2 focus:ring-agro-accent-emerald focus:ring-offset-2 focus:ring-offset-agro-bg-primary;
  }
  
  /* Botões secundários */
  .btn-secondary {
    @apply bg-agro-bg-secondary text-agro-text-primary border border-agro-border-primary font-semibold rounded-2xl px-6 py-3 hover:bg-agro-bg-card transition-all duration-300 focus:outline-none focus:ring-2 focus:ring-agro-accent-sky focus:ring-offset-2 focus:ring-offset-agro-bg-primary;
  }
  
  /* Botões de acento */
  .btn-accent-sky {
    @apply bg-gradient-to-r from-agro-accent-sky to-agro-accent-sky-light text-white font-semibold rounded-2xl px-6 py-3 hover:shadow-lg transition-all duration-300 focus:outline-none focus:ring-2 focus:ring-agro-accent-sky focus:ring-offset-2 focus:ring-offset-agro-bg-primary;
  }
  
  .btn-accent-amber {
    @apply bg-gradient-to-r from-agro-accent-amber to-agro-accent-amber-light text-white font-semibold rounded-2xl px-6 py-3 hover:shadow-lg transition-all duration-300 focus:outline-none focus:ring-2 focus:ring-agro-accent-amber focus:ring-offset-2 focus:ring-offset-agro-bg-primary;
  }
  
  /* Efeito glass */
  .glass {
    @apply bg-agro-bg-glass backdrop-blur-md border border-agro-border-primary;
  }
  
  /* Gradientes de texto */
  .text-gradient-agro {
    @apply bg-gradient-to-r from-agro-accent-emerald via-agro-accent-sky to-agro-accent-amber bg-clip-text text-transparent;
  }
  
  /* Sombras premium */
  .shadow-agro {
    @apply shadow-xl shadow-agro-accent-emerald/10;
  }
  
  /* Animações suaves */
  .transition-agro {
    @apply transition-all duration-300 ease-in-out;
  }
  
  /* Estados de hover */
  .hover-agro {
    @apply hover:shadow-agro hover:scale-105 transition-agro;
  }
  
  /* Badge Accent Green */
  .badge-accent-green {
    @apply inline-flex items-center px-2 py-1 rounded-full text-xs font-medium bg-emerald-100 text-emerald-800 border border-emerald-200;
  }
  
  /* Icon Premium */
  .icon-premium {
    @apply transition-all duration-300 group-hover:scale-110;
  }

  /* AGROSYNC GLOBAL UTILITIES */
  
  /* Card Global */
  .card {
    @apply bg-agro-bg-card backdrop-blur-sm border border-agro-border-primary rounded-2xl shadow-xl;
  }
  
  /* Glass Effect */
  .glass {
    @apply bg-agro-bg-glass backdrop-blur-md border border-agro-border-secondary;
  }
  
  /* Button Primary */
  .btn-primary {
    @apply px-6 py-3 bg-agro-accent-emerald text-agro-text-primary font-semibold rounded-xl hover:bg-agro-accent-emerald-dark transition-all duration-300 shadow-lg hover:shadow-xl;
  }
  
  /* Button Secondary */
  .btn-secondary {
    @apply px-6 py-3 bg-transparent border border-agro-border-primary text-agro-text-secondary font-medium rounded-xl hover:bg-agro-bg-card transition-all duration-300;
  }
  
  /* Form Field */
  .form-field {
    @apply w-full px-4 py-3 bg-agro-bg-card border border-agro-border-primary rounded-xl text-agro-text-primary placeholder-agro-text-tertiary focus:border-agro-accent-emerald focus:ring-2 focus:ring-agro-accent-emerald/20 transition-all duration-300;
  }
  
  /* Input Premium */
  .input-premium {
    @apply w-full px-4 py-3 bg-white/95 backdrop-blur-sm border border-slate-200/50 rounded-xl text-slate-800 placeholder-slate-500 focus:ring-2 focus:ring-emerald-500/20 focus:border-emerald-500 transition-all duration-300;
  }

  .btn-danger {
    @apply px-6 py-3 bg-red-600 text-white font-medium rounded-lg hover:bg-red-700 transition-colors duration-200;
  }

  /* Card Styles */
  .card {
    @apply bg-white rounded-2xl shadow-card border border-slate-200 p-6;
  }

  .card-hover {
    @apply bg-white rounded-2xl shadow-card border border-slate-200 p-6 hover:shadow-elevated transition-shadow duration-300;
  }

  /* Input Styles */
  .input-primary {
    @apply w-full px-4 py-3 border border-slate-300 rounded-lg focus:ring-2 focus:ring-slate-500 focus:border-transparent transition-colors duration-200;
  }

  .input-error {
    @apply w-full px-4 py-3 border border-red-300 rounded-lg focus:ring-2 focus:ring-red-500 focus:border-transparent transition-colors duration-200;
  }

  /* Badge Styles */
  .badge {
    @apply inline-flex items-center px-3 py-1 rounded-full text-sm font-medium;
  }

  .badge-primary {
    @apply bg-slate-100 text-slate-800;
  }

  .badge-success {
    @apply bg-emerald-100 text-emerald-800;
  }

  .badge-warning {
    @apply bg-amber-100 text-amber-800;
  }

  .badge-danger {
    @apply bg-red-100 text-red-800;
  }

  /* Table Styles */
  .table-container {
    @apply bg-white rounded-2xl shadow-card overflow-hidden border border-slate-200;
  }

  .table-header {
    @apply bg-slate-50 px-6 py-4 text-left text-sm font-semibold text-slate-800;
  }

  .table-cell {
    @apply px-6 py-4 text-sm text-slate-600;
  }

  .table-row {
    @apply hover:bg-slate-50 transition-colors duration-200;
  }

  /* Form Styles */
  .form-group {
    @apply mb-6;
  }

  .form-label {
    @apply block text-sm font-medium text-slate-700 mb-2;
  }

  .form-error {
    @apply mt-2 text-sm text-red-600;
  }

  .form-help {
    @apply mt-2 text-sm text-slate-500;
  }

  /* Navigation Styles */
  .nav-link {
    @apply flex items-center space-x-2 px-3 py-2 rounded-lg text-sm font-medium transition-colors duration-200;
  }

  .nav-link-active {
    @apply text-slate-800 bg-slate-100;
  }

  .nav-link-inactive {
    @apply text-slate-600 hover:text-slate-800 hover:bg-slate-50;
  }

  /* Modal Styles */
  .modal-overlay {
    @apply fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50;
  }

  .modal-content {
    @apply bg-white rounded-2xl shadow-elevated max-w-md w-full mx-4 p-6;
  }

  /* Loading States */
  .loading-spinner {
    @apply animate-spin rounded-full border-b-2 border-slate-600;
  }

  .loading-text {
    @apply text-slate-600 text-center py-8;
  }

  /* Status Indicators */
  .status-indicator {
    @apply inline-flex items-center px-2 py-1 rounded-full text-xs font-medium;
  }

  .status-online {
    @apply bg-emerald-100 text-emerald-800;
  }

  .status-offline {
    @apply bg-slate-100 text-slate-800;
  }

  .status-pending {
    @apply bg-amber-100 text-amber-800;
  }

  .status-error {
    @apply bg-red-100 text-red-800;
  }
}

/* Utility Classes */
@layer utilities {
  .text-balance {
    text-wrap: balance;
  }

  .line-clamp-2 {
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  .line-clamp-3 {
    display: -webkit-box;
    -webkit-line-clamp: 3;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  .scrollbar-hide {
    -ms-overflow-style: none;
    scrollbar-width: none;
  }

  .scrollbar-hide::-webkit-scrollbar {
    display: none;
  }
}

/* Animations */
@keyframes fadeIn {
  from {
    opacity: 0;
    transform: translateY(10px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes slideIn {
  from {
    opacity: 0;
    transform: translateX(-20px);
  }
  to {
    opacity: 1;
    transform: translateX(0);
  }
}

@keyframes scaleIn {
  from {
    opacity: 0;
    transform: scale(0.95);
  }
  to {
    opacity: 1;
    transform: scale(1);
  }
}

.animate-fade-in {
  animation: fadeIn 0.3s ease-out;
}

.animate-slide-in {
  animation: slideIn 0.3s ease-out;
}

.animate-scale-in {
  animation: scaleIn 0.3s ease-out;
}

/* Responsive Design */
@media (max-width: 640px) {
  .card {
    @apply p-4;
  }
  
  .modal-content {
    @apply mx-2 p-4;
  }
}

/* Print Styles */
@media print {
  .no-print {
    display: none !important;
  }
  
  .print-only {
    display: block !important;
  }
}

/* Focus Styles for Accessibility */
*:focus {
  outline: 2px solid #475569;
  outline-offset: 2px;
}

/* High Contrast Mode Support */
@media (prefers-contrast: high) {
  .card {
    border-width: 2px;
  }
  
  .btn-primary {
    border: 2px solid currentColor;
  }
}

/* Reduced Motion Support */
@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}
