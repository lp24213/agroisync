import { SettingsPage } from '@/components/settings/settings-page'

export const metadata = {
  title: 'Configurações - AgroSync',
  description: 'Gerencie seu perfil, temas, idioma e integrações na plataforma AgroSync.',
}

export default function SettingsPageRoute() {
  return <SettingsPage />
}
