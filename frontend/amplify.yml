version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "🚀 AGROTM - Build AWS Amplify (Next.js Standalone)"
        - node -v && npm -v
        - npm ci
    build:
      commands:
        - echo "🔨 Executando build standalone..."
        - npm run build
        - echo "NEXT_PUBLIC_API_URL=$NEXT_PUBLIC_API_URL"
    postBuild:
      commands:
        - echo "✅ Build standalone concluído"
  artifacts:
    baseDirectory: .next/standalone
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*
      - ~/.npm/**/*
