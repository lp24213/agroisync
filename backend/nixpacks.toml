[phases.setup]
nixPkgs = ["nodejs_20", "python3", "make", "gcc", "bash"]

[phases.install]
cmds = ["chmod +x build.sh", "npm ci --only=production"]

[phases.build]
cmds = ["./build.sh"]

[start]
cmd = "npm start" 