{
  "id": null,
  "title": "Admin KPIs",
  "panels": [
    {
      "type": "stat",
      "title": "Total Users",
      "targets": [{ "expr": "sum(admin_users_total)", "legendFormat": "users" }],
      "datasource": "Prometheus"
    },
    {
      "type": "stat",
      "title": "Active Sessions",
      "targets": [{ "expr": "sum(admin_active_sessions)", "legendFormat": "sessions" }],
      "datasource": "Prometheus"
    }
  ],
  "schemaVersion": 16,
  "version": 1,
  "refresh": "10s"
}
