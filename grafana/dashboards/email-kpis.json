{
  "id": null,
  "title": "Email KPIs",
  "panels": [
    {
      "type": "stat",
      "title": "Emails Sent",
      "targets": [{ "expr": "sum(emails_sent_total)", "legendFormat": "sent" }],
      "datasource": "Prometheus"
    },
    {
      "type": "stat",
      "title": "Delivery Errors",
      "targets": [{ "expr": "sum(email_delivery_errors_total)", "legendFormat": "errors" }],
      "datasource": "Prometheus"
    }
  ],
  "schemaVersion": 16,
  "version": 1,
  "refresh": "10s"
}
