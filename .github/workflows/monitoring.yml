# DISABLED - Monitoring Workflow
# This workflow is temporarily disabled to focus on deployment
name: Monitoring

on:
  # DISABLED - Only manual trigger
  workflow_dispatch:
    inputs:
      enable_monitoring:
        description: 'Enable monitoring'
        required: true
        default: 'false'
        type: boolean

# DISABLED - All jobs commented out
# jobs:
#   monitoring:
#     name: Monitoring Checks
#     runs-on: ubuntu-latest
#     if: ${{ github.event.inputs.enable_monitoring == 'true' }}
#     steps:
#       - name: Skip monitoring
#         run: echo "Monitoring disabled for deployment focus"
