# DISABLED - Security Audit Workflow
# This workflow is temporarily disabled to focus on deployment
name: AGROTM.SOL Security Audit

on:
  # DISABLED - Only manual trigger
  workflow_dispatch:
    inputs:
      enable_security_audit:
        description: 'Enable security audit'
        required: true
        default: false
        type: boolean

jobs:
  skip-audit:
    name: Skip Security Audit
    runs-on: ubuntu-latest
    if: ${{ github.event.inputs.enable_security_audit == false }}
    steps:
      - name: Skip security audit
        run: echo "Security audit disabled for deployment focus"
