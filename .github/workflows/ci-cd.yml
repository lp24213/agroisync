# DISABLED - CI/CD Pipeline
# This workflow is temporarily disabled to focus on deployment
name: AGROTM.SOL CI/CD Pipeline

on:
  # DISABLED - Only manual trigger
  workflow_dispatch:
    inputs:
      enable_cicd:
        description: 'Enable CI/CD pipeline'
        required: true
        default: false
        type: boolean

env:
  VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
  VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
  VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID_PROD }}
  RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
  RAILWAY_SERVICE: agrotm-solana
  MONGODB_URI: ${{ secrets.MONGODB_URI }}
  JWT_SECRET: ${{ secrets.JWT_SECRET }}

jobs:
  skip-cicd:
    name: Skip CI/CD Pipeline
    runs-on: ubuntu-latest
    if: ${{ github.event.inputs.enable_cicd == false }}
    steps:
      - name: Skip CI/CD
        run: echo "CI/CD pipeline disabled for deployment focus" 