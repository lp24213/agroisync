# DISABLED - CI/CD Simple
# This workflow is temporarily disabled to focus on deployment
name: CI/CD Simple

on:
  # DISABLED - Only manual trigger
  workflow_dispatch:
    inputs:
      enable_cicd_simple:
        description: 'Enable CI/CD simple'
        required: true
        default: false
        type: boolean

jobs:
  skip-cicd-simple:
    name: Skip CI/CD Simple
    runs-on: ubuntu-latest
    if: ${{ github.event.inputs.enable_cicd_simple == false }}
    steps:
      - name: Skip CI/CD Simple
        run: echo "CI/CD simple disabled for deployment focus"
