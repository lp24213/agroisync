# DISABLED - Using ci-cd.yml instead
# This workflow is disabled to avoid conflicts with the main CI/CD pipeline
name: DISABLED - CI/CD Simple

on:
  # Disabled - no triggers
  workflow_dispatch:
    # Manual trigger only for emergencies
    inputs:
      emergency:
        description: 'Emergency deployment only'
        required: true
        default: 'false'

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow Disabled
        run: |
          echo "This workflow is disabled to avoid conflicts"
          echo "Use ci-cd.yml instead"
          exit 0
